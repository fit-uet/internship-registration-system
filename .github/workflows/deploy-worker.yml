name: Deploy Cloudflare Worker (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: worker-deploy
  cancel-in-progress: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Cloudflare Worker deploy is disabled. Render is the primary backend deployment target."
